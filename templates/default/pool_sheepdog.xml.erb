<pool type="sheepdog">
  <name><%= @name -%></name>
  <source>
    <name><%= @options['source']['name'] -%></name>
    <host name='<%= @options['source']['host']['name'] -%>' port='<%= @options['source']['host']['port'] -%>'/>
  </source>
</pool>
